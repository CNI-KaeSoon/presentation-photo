# 07. 개발자 노트

코드를 수정할 때는 결과가 조용히 달라지는 계약부터 확인합니다.

## 핵심 불변식

1. `02_작업장/slide_tool/index.html`의 `COLOR_CORE`와 `05_스크립트/slide_color.py`는 같은 입력에 같은 픽셀 결과를 내야 합니다.
2. `--workers 1`은 기준 직렬 경로입니다. 병렬 경로의 출력 순서와 바이트 결과가 같아야 합니다.
3. 이미지 저장 키 `../<그룹>/img/<파일>`과 `data.js`의 `file` 값은 그대로 유지합니다.
4. 원본 사진은 읽기 전용입니다.
5. 패키지 내부 경로는 현재 작업 디렉터리가 아니라 `Path(__file__)` 또는 계획 파일 위치를 기준으로 계산합니다.
6. `workflow.js`는 `heartbeat.js` 바로 다음에 둡니다. `index.html`의 그룹 이름 변경 UI를 수정하더라도 COLOR_CORE 블록은 건드리지 않으며 sha256은 `d06f4a4ba88c58aa976983999f153f5ffd9986b78ff73787456d0005cbd50434`입니다.

## 브라우저 워크플로 API 계약

- 서버는 `Host`를 모든 요청에서, `Origin`을 모든 변경 요청에서, `X-Workflow-Token`을 `/api/token` 외 모든 API에서 검증합니다.
- 브라우저는 `GET /api/token` 뒤 상태·업로드·폴더 열기·그룹 이름 변경·사진 준비·PDF 생성·잡 조회·잡 취소 API를 사용합니다. 그룹 이름 변경에는 현재·새 basename만 보내며 경로는 보내지 않습니다.
- 준비와 PDF는 동시에 하나만 실행하는 잡입니다. `workflow.js`는 700ms 폴링으로 단계와 증분 로그를 읽고, 잡 실행 중에는 서버 워치독이 종료하지 않습니다.
- `regroup=true`는 그룹명과 localStorage 키를 어긋나게 할 수 있으므로 자체 DOM 확인 모달을 반드시 거칩니다.
- 서버 응답의 파일명·상세 오류·로그는 `innerHTML`이 아니라 `textContent`로 표시합니다.

## 색연산 계약

색 코어를 바꾸면 다음을 함께 검토합니다.

- `02_작업장/slide_tool/index.html`: 슬라이더 정의와 JavaScript 계산
- `05_스크립트/slide_color.py`: 기본값과 Python 계산
- `05_스크립트/test_color_parity.py`: 경계값 회귀 케이스

자동 레벨의 float 정밀도와 half-up 반올림은 픽셀 경계에서 결과를 바꿉니다. 임의로 단순화하지 마세요.

## 백업과 경로 계약

백업 JSON의 localStorage 값은 문자열로 한 번 더 직렬화되어 있습니다. 코너 순서는 `[TL, TR, BL, BR]`이고 좌표는 0~1로 정규화됩니다.

`worktree.json` v2는 계획 파일 기준 POSIX 상대경로를 저장합니다. 읽을 때 현재 운영체제 경로로 해석하며, v1 절대경로 계획은 기존 경로 우선·이동 위치 폴백을 유지합니다.

## 렌더 순서

```text
원본 읽기
→ 네 모서리 원근 보정
→ 선택적 inset 크롭
→ 수동 색보정
→ 선택적 LED 정리
→ PDF 페이지 저장
```

순서를 바꾸면 화면과 PDF가 달라질 수 있습니다.

## 회귀 게이트

```bash
python3 05_스크립트/test_color_parity.py
python3 05_스크립트/test_parallel_parity.py
python3 05_스크립트/test_security.py
python3 05_스크립트/test_serve_tool.py
python3 05_스크립트/test_workflow_api.py
python3 05_스크립트/check_index_integrity.py
python3 05_스크립트/lint_bat.py 시작하기.bat 00_시작/시작하기.bat
python3 05_스크립트/check_portable.py
python3 05_스크립트/check_docs_paths.py
```

## 파일별 영향

| 파일 | 변경 영향 |
|---|---|
| `00_시작/envcheck.py` | 가상환경 탐색과 실행 가능 판정 |
| `00_시작/launch.py` | 사람용 작업 순서와 상태 캐시 사용 |
| `00_시작/serve_tool.py` | 정적 서버, 3중 API 검증, 단일 잡 수명 |
| `02_작업장/slide_tool/workflow.js` | 런타임 작업 순서 패널과 토큰·폴링 클라이언트 |
| `02_작업장/slide_tool/gen_manifest.py` | 브라우저 경로 키와 사진 순서 |
| `05_스크립트/init_worktree.py` | 그룹 구조와 이동 가능한 계획 형식 |
| `05_스크립트/prepare_photos.py` | 원본 매칭, stem 보존, 축소본 생성 |
| `05_스크립트/export_pdf.py` | 원근·색·페이지 순서와 최종 PDF |
| `05_스크립트/parallel_map.py` | 병렬 실행 중 결정적 결과 순서 |
| `05_스크립트/check_index_integrity.py` | COLOR_CORE 해시와 스크립트 앵커 무결성 |

## 출처

이 도구는 실제 컨퍼런스 발표사진 수백 장을 정리하며 만들고 검증했습니다.
